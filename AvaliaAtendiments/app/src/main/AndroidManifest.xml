<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.avaliaatendiments">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AvaliaActivity" />
        <activity android:name=".Cadastro_Func_Activity" />
        <activity android:name=".lstFunc_Activity" />
        <activity
            android:name=".CardColab"
            android:label="@string/title_activity_card_colab"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CardColaborador"></activity>
    </application>

</manifest>